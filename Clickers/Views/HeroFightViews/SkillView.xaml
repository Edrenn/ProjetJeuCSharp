<UserControl x:Class="Clickers.Views.HeroFightViews.SkillView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Clickers.Views.HeroFightViews"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button x:Name="SkillLaunchButton" Content="Skill" />
        <TextBlock x:Name="CounterTB" Text="{Binding UseCounter,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,10,0"/>
    </Grid>
</UserControl>
